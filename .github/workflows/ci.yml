name: CI
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  validate:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        runtime: [minimum, current]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: pnpm/action-setup@v6.1.0
      - uses: actions/setup-node@v7.0.0
        if: matrix.runtime == 'current'
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - uses: actions/setup-node@v7.0.0
        if: matrix.runtime == 'minimum'
        with:
          node-version: '26.0.0'
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build once and validate ESLint 10.10.0
        run: pnpm check
